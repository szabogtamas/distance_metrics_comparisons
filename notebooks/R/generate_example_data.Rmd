---
title: "`r params$report_title`"
author: "`r params$report_author`"
date: "`r params$report_time`"
output:
  html_document:
    toc: True
params:
  report_title: "Generate example data"
  report_author: "Anonymus"
  report_time: !r format(Sys.Date(), format="%d/%m/%Y")
---

## Setup

```{r}
library(dplyr)
library(tidyr)
library(tibble)
```

```{r}
example_df <- data.frame(Entity = paste("e", seq(10001, 30000), sep="_"))

for(cl in paste("cat", seq(1000, 1050), sep="_")){
  example_df[[cl]] <- sample(c(0, 1), 20000, replace=TRUE)
}

head(example_df)
```



